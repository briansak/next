services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: next
    ports:
      - "5432:5432"
    volumes:
      - next_pgdata:/var/lib/postgresql/data

volumes:
  next_pgdata:
